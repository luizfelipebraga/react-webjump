import styled from 'styled-components';

export const Container = styled.header`
  width: 100%;
  background: #CB0D1F;
`;

export const Nav = styled.nav`
  max-width: 1200px;
  display: flex;
  gap: 1rem;
  align-items: center;
`;